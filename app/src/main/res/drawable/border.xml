<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/border_corner_radius" />
    <solid android:color="@color/color_background_view" />
    <size
        android:width="@dimen/border_size_width"
        android:height="@dimen/border_size_heigth" />
    <stroke
        android:width="@dimen/border_stroke_width"
        android:color="@color/color_black" />
</shape>